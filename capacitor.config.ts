import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'camera.webrtc.angular13',
  appName: 'camera-webrtc-angular13',
  webDir: 'dist/camera-webrtc-angular13',
  server: {
    androidScheme: 'https'
  }
};

export default config;
